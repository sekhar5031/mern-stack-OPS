apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-mern-stack.fullname" . }}
  labels:
    {{- include "helm-mern-stack.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-mern-stack.selectorLabels" . | nindent 4 }}
